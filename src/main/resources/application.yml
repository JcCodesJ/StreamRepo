spring:
  config:
    activate:
      on-profile: justin
  datasource:
    url: jdbc:mysql://localhost:3306/user
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
---
spring:
  config:
    activate:
      on-profile: justin2
